{
  "name": "puppeteer-heroku-master-2",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "heroku-postbuild": "npm install --only=dev && npm install && npm cache clean --force && npm install --save puppeteer"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^4.21.1",
    "puppeteer": "^23.5.3"
  }
}
